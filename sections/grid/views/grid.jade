doctype html
div(
  id = 'game-{{ ngModel.gameSize }}'
)
  .grid-container
    .grid-cell(
      ng-repeat = 'cell in ngModel.grid track by $index'
    )
  .tile-container
    div(
      tile
      ng-model = 'tile'
      ng-repeat = 'tile in ngModel.tiles track by $id(tile.id || $index)'
    )
